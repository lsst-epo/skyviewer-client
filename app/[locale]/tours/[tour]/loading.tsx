import { FunctionComponent } from "react";
import LoadingSpinner from "@/components/primitives/LoadingSpinner";

const TourLoading: FunctionComponent = () => {
  return <LoadingSpinner />;
};

export default TourLoading;
