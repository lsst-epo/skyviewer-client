import { FunctionComponent } from "react";
import LoadingSpinner from "@/components/primitives/LoadingSpinner";

const RootLoading: FunctionComponent = () => {
  return <LoadingSpinner fillWindow />;
};

export default RootLoading;
